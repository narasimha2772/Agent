## ACP Money System (Preview Mode)

Quickstart:
- cp env.example .env
- docker compose -f docker/docker-compose.yml up -d --build
- Open Master API: http://localhost:3000/acp/health, Dashboard: http://localhost:4000
- Default mode: preview. No secrets required; agents generate preview artifacts.
